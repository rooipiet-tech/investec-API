---
id: L-0002
class: architecture
created: 2026-06-25
run: email-payment-approval-loop
tags: [credentials, feature-flags, dead-gate]
severity: major
applies_to: [output-reviewer, risk-security, planner, spec-writer]
status: active
---
statement: A credential/flag presence-CHECKED at a gate but never WIRED into the client performing the gated op is a recurring latent gap: the gate passes while the privileged call runs with wrong/default creds.
evidence: Backed by this run's reviewer findings/spec criteria (see .loop/review.json, risk.json, spec.json).
catch_next_time: Trace the gated resource from gate to call site; require a test that the privileged (mocked) path consumes the write-scoped credential, not the default. Treat 'flag parsed but never consumed' as its own defect class.
